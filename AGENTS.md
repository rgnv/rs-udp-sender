# AGENTS.md — rs-udp-sender

**Generated:** 2026-05-08
**Stack:** Rust workspace, raw sockets (nix), clap, rasn-snmp, bytes

## OVERVIEW
Rust rewrite of [Cribl's udp-sender](https://github.com/criblio/udp-sender). 3-binary workspace: UDP sender with per-packet IP/port spoofing via raw sockets, packet generator, SNMP trap generator (v1/v2c/v3). Binary protocol over stdin/stdout.

## STRUCTURE
```
crates/
├── udp-sender/src/        # Core library + main binary
├── packet-generator/src/  # CLI: generates binary protocol packets
└── snmp-trap-generator/src/ # CLI: generates SNMP traps in protocol format
```

## WHERE TO LOOK
| Task | Location |
|------|----------|
| Binary protocol parsing | `crates/udp-sender/src/protocol.rs` |
| IP/UDP header construction | `crates/udp-sender/src/packet.rs` |
| Raw socket send | `crates/udp-sender/src/sender.rs` |
| ND-JSON logging | `crates/udp-sender/src/logger.rs` |
| SNMP v1/v2c/v3 | `crates/udp-sender/src/snmp.rs` |
| CLI flags | `crates/udp-sender/src/main.rs` |
| Golden test vectors | `/root/udp-sender/golden_harness.go` (Go reference) |
| Release CI | `.github/workflows/release.yml` |

## CONVENTIONS
- **Error types**: `thiserror` for library errors, `anyhow` for binaries
- **Tests**: inline `#[cfg(test)]` modules, no separate test files
- **Raw socket tests**: all gated with `#[ignore = "requires root/CAP_NET_RAW"]`
- **Logging**: custom ND-JSON logger matching Go's format (lowercase levels, no timestamp, flattened fields)
- **Protocol format**: exactly matches Go binary protocol (magic 0xC1 0x21 0xB1)

## ANTI-PATTERNS
- **NEVER** suppress type errors with `@ts-ignore` / `as` casts on raw socket types
- **NEVER** add async to the core protocol/sender pipeline — synchronous by design
- **NEVER** change the binary protocol format without updating both Go and Rust generators
- **NEVER** commit `.sisyphus/` plans or internal tooling metadata

## UNIQUE STYLES
- `PacketSender` trait (Go interface parity) for sender implementations
- `ProtocolStream<R: Read>` implements `Iterator` for packet-by-packet processing
- SNMP v3 key derivation uses manual HMAC (no external crypto lib)

## COMMANDS
```bash
make build    # cargo build --release --workspace
make test     # cargo test --workspace
make lint     # cargo clippy --workspace -- -D warnings
make fmt      # cargo fmt
make release  # cross-compile + package
```

## NOTES
- Raw socket sending requires `CAP_NET_RAW` (Linux) or root
- IPv6 detection is optional — sender degrades gracefully
- Golden test vectors generated by Go reference harness at `/root/udp-sender/golden_harness.go`
- AI-assisted development — see [AI.md](./AI.md)
